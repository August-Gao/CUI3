import { rgb } from "polished";

export enum ThemeEunm {
  DARK = "dark",
  LIGHT = "light",
}

export const baseColors = {
  black: rgb(0, 0, 0),
  current: "currentColor",
  inherit: "inherit",
  transparent: "transparent",
  white: rgb(255, 255, 255),
};
